# Calculator (Qt/QML)

Калькулятор с поддержкой скобок и секретным режимом, реализованный на Qt Quick (QML) и C++.

## ✨ Функциональность
- Базовые арифметические операции: `+`, `−`, `×`, `÷`
- Скобки `()` с интеллектуальной логикой открытия/закрытия в зависимости от контекста
- Кнопка `+/-` для смены знака последнего числа
- Проценты `%` — вычисление процента от последнего числа в выражении
- Поддержка длинных выражений с отображением результата до 10 знаков
- Секретный режим: долгое нажатие (4 сек) на `=` → ввод кода `123` в течение 5 сек → переход в секретное меню

## 🎨 Дизайн
- Круглые кнопки (диаметр 62px, радиус = 50%)
- Цветовая схема:
  - Цифровые кнопки: `#B0D1D8` / при нажатии `#04BFAD`
  - Операционные кнопки (`+`, `−`, `×`, `÷`, `%`, `()`): `#0889A6` / при нажатии `#F7E425`
  - Фон приложения: `#024873`
  - Дисплей: `#04BFAD` с закруглёнными нижними углами (радиус 32px)
- Типографика: шрифт Open Sans, DemiBold

## 🔒 Секретный режим
1. Долгое нажатие **ровно 4 секунды** на кнопку `=`
2. В течение **5 секунд** ввести последовательность `1` → `2` → `3`
3. При успешном вводе — мгновенный переход во второе окно
4. При таймауте (>5 сек) или неверной последовательности — тихий сброс без визуальных эффектов
5. Интерфейс калькулятора **не изменяется** во время ввода кода (нет индикаторов режима)

## 🛠 Сборка и запуск

### Требования
- Qt 5.12+ (модули: Core, Gui, Quick)
- CMake 3.16+
- Компилятор с поддержкой C++17

### Сборка
```bash
mkdir build && cd build
cmake ..
cmake --build .
```

### Запуск
```bash
./bin/calculator-qml
```

> 💡 Все QML-ресурсы встроены в бинарник через `calculator.qrc`. Для запуска требуется только один исполняемый файл — внешние `.qml` файлы не нужны.

## 📁 Структура проекта
```
calculator-qml/
├── CMakeLists.txt          # Конфигурация сборки
├── main.cpp                # Точка входа (загрузка через qrc:/main.qml)
├── calculator.qrc          # Манифест ресурсов
├── main.qml                # Корневой компонент (ApplicationWindow + StackView)
├── Calculator.qml          # Основной экран калькулятора с логикой секретного режима
├── SecretMenu.qml          # Второй экран: надпись «Секретное меню» + кнопка «Назад»
└── components/
    ├── KeypadButton.qml    # Стандартная кнопка
    └── LongPressButton.qml # Кнопка с поддержкой долгого нажатия (4000 мс)
```

## ✅ Проверка секретного режима
1. Запустите калькулятор
2. Удерживайте кнопку `=` ровно 4 секунды (визуально интерфейс не меняется)
3. В течение 5 секунд последовательно нажмите: `1` → `2` → `3`
4. При успешном вводе откроется второе окно с надписью «Секретное меню» и кнопкой «Назад»
5. При задержке >5 сек или неверной последовательности — режим автоматически сбрасывается без визуальных эффектов

## ⚙️ Технические особенности реализации
- Таймер на 5 секунд для ограничения времени ввода кода (`Timer { interval: 5000 }`)
- Проверка корректности последовательности на лету (только `1`→`2`→`3`)
- Отсутствие визуальных индикаторов секретного режима (полное соответствие ТЗ)
- Самодостаточный бинарник благодаря встроенным ресурсам (`.qrc`)
- Чистая архитектура: логика отделена от UI, все состояния инкапсулированы в `Calculator.qml`